Main
 (
Master
) — основная ветка, которая выкатывается на продакшен.
Develop
 — рабочая ветка, куда попадают все принятые изменения.
Feature/Task1
 — функциональная ветка для внедрения фичи, берется из ветки develop.
Release/R1
 — ветка для подготовки релиза.
Hotfix/H2
 — ветка, которая используется для внесения срочных исправлений, берется от ветки main.
